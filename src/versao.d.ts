/**
 * Carimbo do build, trocado por texto no bundle (ver `define` no vite.config).
 *
 * Em `npm run dev` ele nao existe: quem le usa o `typeof` antes de tocar.
 */
declare const __VERSAO__: string;
